import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

const Header = () => (
  <header className="Header">
    <h1>Stellar Command Academy</h1>
  </header>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
